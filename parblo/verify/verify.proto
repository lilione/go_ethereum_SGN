// cd src/go/grpc/verify
// protoc --go_out=plugins=grpc:./ verify.proto

syntax = "proto3";
package verify;

message Request {
    bytes payload = 1;
}

message Response {
    bool valid  = 1;
}

service VerifyTx {
    rpc Evaluate(Request) returns (Response) {}
}